def high_and_low(numbers):
    x = [int(item) for item in numbers.split()]
    return f"{max(x)} {min(x)}"


# Best Practices
def high_and_low(numbers):
    x = [int(item) for item in numbers.split()]
    return f"{max(x)} {min(x)}"
